name: Fetchflow
slug: fetchflow
access: public

description: "Lets you integrate Supportbee with fetchflow!"

category: Integration

tags: 
- fetchflow
- billing
- invoice

developer: 
  name: Shrinath M Aithal
  email: shrinath_m2@yahoo.co.in
  twitter: "@shrinath_m2"
  github: shrinathaithal

# The following config keys are used by Action Handlers
# Set button 'overlay' to true, if you want to have an overlay
# The valid values for screens are [ticket, all, unassigned, my, groups]
# Remove the following section if you do not want to define Action Handlers
 
action:
  button:
    overlay: false
    screens: 
    - ticket
    - all
    - unassigned
    label: Send To Fetchflow
